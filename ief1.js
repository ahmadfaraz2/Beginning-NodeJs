// ---------------------Immediately Executing Function--------------------

// We can execute a function immediately after we define it.
// Simply wrap the function in parentheses () and invoke it.


(function foo() {
    console.log("foo was executed!");
})();

